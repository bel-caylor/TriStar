{# src/_includes/components/sidebar-nav.njk #}

{% from "components/sidebar-link.njk" import linkItem %}

<aside class="w-20 bg-white border-r border-gray-200 h-screen flex flex-col items-center py-4">

  <!-- Navigation -->
  <nav class="flex-1">
    <ul class="flex flex-col items-center space-y-6">

      {# ─── Personal ────────────────────────────────────────────────────── #}
      {{ linkItem("/EmpFindTableLTE.asp?tp=lnav", "fa-user", "Personal") }}

      {# ─── Family ──────────────────────────────────────────────────────── #}
      {{ linkItem("/DepReportLTE.asp",       "fa-home",           "Family") }}

      {# ─── Benefits ───────────────────────────────────────────────────── #}
      {{ linkItem("/BenListLTE.asp",         "fa-notes-medical",  "Benefits") }}

      {# ─── Beneficiaries ───────────────────────────────────────────────── #}
      {{ linkItem("/HrBeneficRptLTE.asp",    "fa-hand-holding-usd", "Beneficiaries") }}

      {# ─── Summary ─────────────────────────────────────────────────────── #}
      {{ linkItem("/ConfirmUnivLTE.asp",     "fa-table",          "Summary") }}

      {# ─── Upload ──────────────────────────────────────────────────────── #}
      {{ linkItem("/UploadLTE.asp",          "fa-upload",         "Upload") }}

    </ul>
  </nav>
</aside>
